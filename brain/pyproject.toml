[project]
name = "savewise-brain"
version = "0.1.0"
description = "SaveWise brain service"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115",
    "openai>=2.53.0",
    "requests>=2.34.2",
    "uvicorn[standard]>=0.30",
]

[dependency-groups]
dev = [
    "httpx>=0.27",
    "pytest>=8.0",
    "ruff>=0.6",
]
