# Windows 11 development environment (PRD §6.10 dev/test path).
#
# On the Raspberry Pi 5 do NOT use this file — see PRD §6.6. There, opencv and
# picamera2 come from apt (python3-opencv, python3-picamera2) and are visible to
# the venv via --system-site-packages. Installing them from PyPI on the Pi wastes
# scarce SD-card space and the PyPI picamera2 build routinely fails.

ultralytics>=8.3.0      # pulls in torch + torchvision (~2-3 GB)
onnxruntime>=1.17       # CPU inference for exported .onnx models
opencv-python>=4.9      # Windows only; apt provides this on the Pi
numpy>=1.26

# ByteTrack's linear-assignment solver. Ultralytics does NOT declare this as a
# hard dependency — it AutoUpdates it over the network on the first .track()
# call. That breaks on an offline Pi (NFR5: no network at run time), and it
# breaks late, at the first tracked frame rather than at startup. Pin it.
lap>=0.5.12

# Test
pytest>=8.0
